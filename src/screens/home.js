import React from 'react';
import styled from 'styled-components';

import { Wrapper, H3, Button } from '../components/common-elements';

const Home = ()=>{
  return(
    <Wrapper>
      <p className="App-intro">Creating React elements with styled components</p>
      <H3>Button</H3>
    </Wrapper>
  )
}

export default Home;
